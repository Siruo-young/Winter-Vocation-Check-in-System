<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.winter_vacation_checkin.mapper.LaUserJoinMapper">

    <resultMap id="BaseResultMap" type="com.example.winter_vacation_checkin.domain.LaUserJoin">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="activityId" column="activity_id" />
            <result property="status" column="status" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="createUser" column="create_user" />
            <result property="updateUser" column="update_user" />
            <result property="isDelete" column="is_delete" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,activity_id,status,create_time,update_time,
        create_user,update_user,is_delete
    </sql>
    <select id="getByActivityId" resultType="long">
        select count(*)
        from la_user_join
        where activity_id = #{activityId} and is_delete =0;
    </select>
    <select id="getUserJoinByNameTypePage"
            resultType="com.example.winter_vacation_checkin.vo.UserActivityListVo">
        select a.name as activity_name,
               a.status as activity_status,
               u.name as user_name,
               a.start_time as start_time,
               a.end_time as end_time
        from la_user_join as uj
        join la_activity as a on uj.activity_id = a.id
        join la_user as u on uj.user_id = u.id
        where u.is_delete = 0 and uj.is_delete = 0
        <if test="name != null">
            and a.name like concat('%',#{name},'%')
        </if>
        <if test="type != null">
            and a.status = #{status}
        </if>
    </select>
</mapper>
